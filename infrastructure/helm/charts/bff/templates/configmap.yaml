apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bff.fullname" . }}-config
  labels:
    {{- include "bff.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
